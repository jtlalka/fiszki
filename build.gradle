/*
 * Gradle main build script
 */
buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.1.2'
    }
}

/*
 * Gradle all projects setup
 */
allprojects {
    repositories {
        jcenter()
    }
}
