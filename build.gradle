/**
 * Gradle main build script
 */
buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.0'
    }
}

/**
 * Gradle all projects setup
 */
allprojects {
    repositories {
        jcenter()
    }
}
